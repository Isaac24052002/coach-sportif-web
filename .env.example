# App
APP_HOST=0.0.0.0
APP_PORT=8000

# CORS (comma separated). Leave empty if the frontend is served by the same domain.
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Database (SQLite file path)
DB_PATH=/app/donnees/fitness_data.db
